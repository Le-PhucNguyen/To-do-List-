import React from 'react';
import ReactDOM from 'react-dom';
import Router from './components/Router'; // Adjust path if Router.js is in a subfolder

ReactDOM.render(<Router />, document.getElementById('root'));
